import streamlit as st
import urllib.parse
import json, base64

st.set_page_config(page_title="XPLØR QR", page_icon="🌐", layout="centered")

# 🌈 Enhanced Futuristic Style (Neon & Motion)
st.markdown("""
<style>
html, body, [class*="css"] {
    background: radial-gradient(circle at 20% 30%, #e3f2fd, #ffffff 60%);
    font-family: 'Segoe UI', sans-serif;
    color: #0d1b2a;
    overflow-x: hidden;
}

/* === HEADER TITLE EFFECT === */
@keyframes glow {
    0% { text-shadow: 0 0 5px #26C6DA, 0 0 10px #0288D1, 0 0 20px #01579B; }
    50% { text-shadow: 0 0 15px #26C6DA, 0 0 30px #0288D1, 0 0 45px #01579B; }
    100% { text-shadow: 0 0 5px #26C6DA, 0 0 10px #0288D1, 0 0 20px #01579B; }
}
h1 {
    text-align: center;
    font-size: 64px;
    font-weight: 900;
    letter-spacing: 2px;
    background: linear-gradient(90deg, #01579B, #00ACC1);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: glow 2s infinite;
    margin-top: 25px;
}

/* SUBHEADINGS */
h3, h4 {
    text-align: center;
    color: #01579B;
    font-weight: 600;
    margin-bottom: 10px;
}

/* DIVIDER */
.divider {
    height: 5px;
    width: 120px;
    background: linear-gradient(90deg, #0288d1, #26c6da, #0288d1);
    margin: 25px auto;
    border-radius: 3px;
    animation: slide 3s infinite linear;
}
@keyframes slide {
    0% { width: 60px; opacity: 0.8; }
    50% { width: 120px; opacity: 1; }
    100% { width: 60px; opacity: 0.8; }
}

/* BUTTONS */
.xplor-button {
    display: block;
    width: 100%;
    background: linear-gradient(135deg, #0288D1, #26C6DA);
    color: white !important;
    padding: 18px;
    margin: 14px 0;
    border: none;
    border-radius: 45px;
    font-size: 20px;
    font-weight: 700;
    text-align: center;
    letter-spacing: 0.6px;
    transition: all 0.3s ease-in-out;
    box-shadow: 0 8px 20px rgba(0,0,0,0.15);
    position: relative;
    overflow: hidden;
}
.xplor-button::before {
    content: "";
    position: absolute;
    top: 0;
    left: -75%;
    width: 50%;
    height: 100%;
    background: rgba(255, 255, 255, 0.25);
    transform: skewX(-25deg);
    transition: 0.5s;
}
.xplor-button:hover::before {
    left: 125%;
}
.xplor-button:hover {
    transform: scale(1.07);
    box-shadow: 0 12px 35px rgba(2,136,209,0.4);
}

/* FADE IN ANIMATION */
.fade-in {
    animation: fadeIn 1.4s ease-in;
}
@keyframes fadeIn {
    from {opacity: 0; transform: translateY(15px);}
    to {opacity: 1; transform: translateY(0); opacity: 1;}
}

/* FOOTER */
.footer {
    text-align: center;
    color: #37474f;
    margin-top: 60px;
    font-size: 16px;
}
.flag {
    width: 28px;
    vertical-align: middle;
}
</style>
""", unsafe_allow_html=True)

# ✅ Use the new Streamlit API
params = st.query_params

# 🚀 Get encoded payload from robot (?data=)
payload_data = params.get("data", None)

# 🧩 Decode payload sent by XPLØRBOT
if payload_data:
    try:
        padded = payload_data + "=" * (-len(payload_data) % 4)
        decoded = json.loads(base64.urlsafe_b64decode(padded.encode()).decode())

        category = decoded.get("category", "Tourist attractions")
        subcategory = decoded.get("subcategory", "")
        suggestions = decoded.get("suggestions", [])
    except Exception:
        st.error("⚠️ Invalid QR data. Please scan again.")
        st.stop()
else:
    st.warning("⚠️ Please scan the QR code generated by XPLØRBOT to view your recommendations.")
    st.stop()

# 🧭 Same suggestion data as robot
SUGGESTIONS = {
    "Museums": ["Biomuseum", "Canal Museum", "Museum of Contemporary Art"],
    "Restaurants": ["Seafood Market", "Tantalo", "Maito", "Fonda Lo Que Hay"],
    "Nature": ["Metropolitan Park", "Taboga Island", "Coiba National Park", "Boquete"],
    "Shopping malls": ["Multiplaza", "Albrook Mall", "Metromall", "Soho Mall"],
    "Budget": ["Hostel Loco Coco Loco", "Hotel Centroamericano", "Hotel Marparaiso"],
    "Luxury": ["Hotel Las Américas", "W Panama", "Trump Tower"],
    "Downtown": ["Hotel El Panamá", "Hotel Riu", "Hotel Marbella"],
    "With pool": ["Sortis Hotel", "Hotel Crowne Plaza", "Bristol Panama"],
    "Taxi": ["Uber", "Cabify", "Panamanian Taxis"],
    "Metro": ["Line 1", "Line 2", "MetroBus Recharges"],
    "Car rental": ["Thrifty", "Hertz", "Dollar Rent A Car"],
    "Ride-sharing apps": ["Uber", "InDrive", "Cabify"],
    "Hospital": ["Punta Pacífica Hospital", "National Hospital", "Santo Tomás Hospital"],
    "Police": ["National Police", "DIJ", "Emergency 104"],
    "Embassy": ["US Embassy", "French Embassy", "Colombian Embassy"],
    "Pharmacy": ["Arrocha", "Metro Plus", "El Javillo"]
}

# 🖥️ HEADER SECTION
st.markdown("<h1 class='fade-in'>XPLØRBOT</h1>", unsafe_allow_html=True)
st.markdown("<div class='divider'></div>", unsafe_allow_html=True)
st.markdown("<h3>Welcome to Panama</h3>", unsafe_allow_html=True)
st.markdown(f"<h4>Category: <b>{category}</b></h4>", unsafe_allow_html=True)
if subcategory:
    st.markdown(f"<h4>Topic: <b>{subcategory}</b></h4>", unsafe_allow_html=True)
st.markdown("<div class='divider'></div>", unsafe_allow_html=True)

# 🌟 RECOMMENDATIONS SECTION
if suggestions:
    st.markdown("<h3 class='fade-in'>Recommended Places</h3>", unsafe_allow_html=True)
    for place in suggestions:
        url = f"https://www.google.com/search?q={urllib.parse.quote(place + ' Panama')}"
        st.markdown(f"<a class='xplor-button fade-in' href='{url}' target='_blank'>{place}</a>", unsafe_allow_html=True)
else:
    fallback = SUGGESTIONS.get(subcategory, [])
    if fallback:
        st.markdown("<h3 class='fade-in'>Recommended Places</h3>", unsafe_allow_html=True)
        for place in fallback:
            url = f"https://www.google.com/search?q={urllib.parse.quote(place + ' Panama')}"
            st.markdown(f"<a class='xplor-button fade-in' href='{url}' target='_blank'>{place}</a>", unsafe_allow_html=True)
    else:
        st.info("No recommendations available. Please rescan your QR.")

# 🌍 FOOTER
st.markdown("""
<div class='footer fade-in'>
    XPLØR © 2025 – Made in Panama 
    <img class='flag' src='https://upload.wikimedia.org/wikipedia/commons/a/ab/Flag_of_Panama.svg'>
</div>
""", unsafe_allow_html=True)
